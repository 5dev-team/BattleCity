pre-commit:
  parallel: true
  commands:
    lint:
      glob: '*.{ts,tsx,scss}'
      run: yarn run lint {staged_files}
commit-msg:
  commands:
    commitlint:
      run: yarn commitlint --edit "{1}"
